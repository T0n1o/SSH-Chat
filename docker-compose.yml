services:
  ssh-chat:
    build: .
    ports:
      - "2222:2222"
    environment:
      # Troque a senha aqui
      SSH_CHAT_PASSWORD: "chat123"
    volumes:
      # Persiste a host key entre reinícios
      - ./host_key:/app/host_key

